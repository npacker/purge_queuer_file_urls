name: 'File URLs Queuer'
type: module
description: 'Purger for file URLs derived from file upload fields.'
package: "Purge - queuers"
core_version_requirement: ^9 || ^10 || ^11
dependencies:
    - drupal:file
    - purge:purge
